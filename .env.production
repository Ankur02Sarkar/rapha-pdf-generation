# Production Environment Configuration for AWS Lambda
# This file contains production-specific environment variables

# Application Settings
ENVIRONMENT=production
DEBUG=false
PROJECT_NAME=RaphaCure PDF Generation API
VERSION=1.0.0

# Security Settings (CHANGE THESE IN PRODUCTION!)
SECRET_KEY=rapha-pdf-gen-prod
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings (Update with your actual domains)
ALLOWED_HOSTS=["*"]

# API Settings
API_V1_STR=/api/v1

# AWS Lambda Settings
LAMBDA_TIMEOUT=30
LAMBDA_MEMORY=1024

# PDF Generation Settings
PDF_MAX_SIZE_MB=10
PDF_TIMEOUT_SECONDS=30

# Logging
LOG_LEVEL=INFO

# AWS Lambda Runtime (automatically set)
AWS_LAMBDA_FUNCTION_NAME=rapha-pdf-generation