# Staging Environment Configuration for AWS Lambda
# This file contains staging-specific environment variables

# Application Settings
ENVIRONMENT=staging
DEBUG=true
PROJECT_NAME=RaphaCure PDF Generation API (Staging)
VERSION=1.0.0-staging

# Security Settings
SECRET_KEY=your-staging-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS Settings (More permissive for testing)
ALLOWED_HOSTS=["*"]

# API Settings
API_V1_STR=/api/v1

# AWS Lambda Settings
LAMBDA_TIMEOUT=30
LAMBDA_MEMORY=1024

# PDF Generation Settings
PDF_MAX_SIZE_MB=10
PDF_TIMEOUT_SECONDS=30

# Logging
LOG_LEVEL=DEBUG

# AWS Lambda Runtime (automatically set)
AWS_LAMBDA_FUNCTION_NAME=rapha-pdf-generation-staging